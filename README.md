## Go Patterns
A simple program to compare code quality, structure and performance of different code patterns at runtime in the context of Golang programming language.

#### To Run Locally
1. Fill values for all fields mentioned in `local.env` file.
2. Rename `local.env` to `.env`.
3. Run `go run main.go`.
